menuconfig ATBM_MENUCONFIG_WIFI6
       tristate "Atbm Wireless Lan"
       default m

if ATBM_MENUCONFIG_WIFI6
config DRIVER_PATH
	string
	option env="DRIVER_PATH"
source "$DRIVER_PATH/Kconfig"
endif
